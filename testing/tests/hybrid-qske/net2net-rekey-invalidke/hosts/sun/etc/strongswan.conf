# /etc/strongswan.conf - strongSwan configuration file

charon {
  load = random nonce aes gcm sha2 pem pkcs1 openssl curve25519 x509 curl nistpqc revocation hmac stroke kernel-netlink socket-default updown
  send_vendor_id = yes
}
